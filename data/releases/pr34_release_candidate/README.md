# AI Economy Ledger dataset release dataset-release:1:5424bda5073c2a1a09cb

This is a deterministic local/CI release candidate. It is not a live publication.

The latest-source-attributed lane may include disclosed records awaiting human review. The verified lane is controlled by explicit private-engine decisions. System validation is never represented as human verification. Coverage is limited to the documented expected matrix and missing values are never converted to zero.
