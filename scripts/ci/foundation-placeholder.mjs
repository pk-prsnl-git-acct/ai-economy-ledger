const target = process.argv[2] ?? "unknown";

console.log(`${target} is intentionally unavailable in the repository-foundation phase.`);
console.log("The runtime scaffold will replace this explicit placeholder with a real quality gate.");
