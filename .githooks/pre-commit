#!/bin/sh
set -eu

echo "Running AI Economy Ledger pre-commit checks..."
NODE_BIN="$(git config --local --get ael.nodePath || command -v node)"
"$NODE_BIN" scripts/ci/validate-repository.mjs --mode=lint
git diff --cached --check
